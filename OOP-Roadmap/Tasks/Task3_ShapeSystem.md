# Task 3 — Shape System

**Description:**  
Create an abstract `Shape` class with a method `area()`. Implement `Circle` and `Rectangle` classes that extend `Shape` and override `area()`.  

**References:**  
[Abstract Classes in Dart](https://dart.dev/guides/language/language-tour#abstract-classes)

**Hints:**  
- Use `override` to implement `area()`  
- Use constructor parameters to set dimensions
