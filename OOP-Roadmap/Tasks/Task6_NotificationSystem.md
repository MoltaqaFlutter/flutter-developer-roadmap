# Task 6 — Notification System

**Description:**  
Implement a `NotificationChannel` interface with classes `EmailNotification`, `SMSNotification`, and `PushNotification`. Each class should have a `send(message)` method.

**References:**  
[Polymorphism in Dart](https://dart.dev/language/class-modifiers#interface)

**Hints:**

- Use a base class or interface to enforce method signature
- Implement different behaviors in each notification type
